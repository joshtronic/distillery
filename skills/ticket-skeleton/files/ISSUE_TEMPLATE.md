<!-- One paragraph: what this is and why it exists. The worker that picks
this up has amnesia: it reads this body and the repo's context file, nothing
else. Paste anything load-bearing -- don't link to it. Restate any decision a
prior discussion made. -->

## Deliverables

1. <!-- Concrete artifact or change, one per numbered item. Exact paths,
   exact names, exact commands. Checkable from the diff. -->

## Out of scope

- <!-- The adjacent work a reasonable agent might do but must not.
   "Nothing else" is a valid entry; skipping the question is not. -->

## Verification

- <!-- How the worker proves it worked before exiting: the command, the
   expected output, the fixture. "Tests pass" only counts naming which. -->
- <!-- What the human reviewer should check that automation can't. -->

## Notes

- <!-- Constraints, gotchas, sharp edges. What could page you at 3am? -->
